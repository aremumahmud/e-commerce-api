// const colorModel = require("../../db/Models/color.Model");

// colorModel.find().then(res => {
//     console.log(res)
// })

module.exports = {
    exp: 2,
    pickup_adress: 'this is the pickup address ooo'
}